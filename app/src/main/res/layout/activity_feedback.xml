<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f3f3f3"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <com.xh.common.view.TitleView
        style="@style/title"
        app:titleText="意见反馈"></com.xh.common.view.TitleView>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="400mm"
        android:layout_margin="30mm"
        android:background="@drawable/shape_bg_white_5dp"
        android:padding="20mm">

        <EditText
            android:id="@+id/et_content"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:gravity="top"
            android:hint="问题说明,您的建议可以让我们更好的发展"
            android:inputType="textMultiLine"
            android:maxLength="500"
            android:scrollbars="vertical"
            android:textColor="@color/colorPrimaryText"
            android:textColorHint="@color/colorSecondaryText"
            android:textSize="22mm" />

        <TextView
            android:id="@+id/tv_num"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:text="0/500" />
    </RelativeLayout>


    <Button
        android:id="@+id/btn_submit"
        style="@style/btn_style"
        android:background="@drawable/selector_btn_blue"
        android:enabled="false"
        android:text="提交" />

</LinearLayout>
